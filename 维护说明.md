# Tulip 个人主页维护说明

这份项目是一个以 GitHub 为内容来源的个人作品集网站。网站展示的是项目总结、图片、README 和人生记录，不直接把 GitHub 的源码文件树展示给访客。

## 1. 本地启动

在项目根目录执行：

```bash
npm install
npm run dev -- --host 127.0.0.1 --port 4173 --strictPort
```

打开 `http://127.0.0.1:4173/`。

如果只想生成部署文件：

```bash
npm run build
```

## 2. 常用修改位置

| 要修改的内容 | 文件 |
| --- | --- |
| 首页、项目列表、项目详情页的布局和文案 | `src/App.jsx` |
| GitHub 项目初始数据、README 初始内容、项目摘要 | `src/productData.js` |
| 个人资料、经历、技能、联系方式 | `src/content/tulip-profile.json` |
| GitHub 导入、README/代码内容读取 | `src/api.js` |
| 页面样式 | `src/styles.css` |
| 头像、项目照片、封面图 | `public/assets/` |

## 3. 新增项目的格式

项目固定数据写在 `src/productData.js` 的 `GITHUB_PROJECTS` 中。每个项目至少要有：

```js
{
  id: "github-example",
  slug: "example",
  name: "项目名称",
  description: "一句话说明项目解决什么问题。",
  summary: "面向访客的项目精华总结，说明目标、做法和产出。",
  highlights: [
    "目标：要解决的问题",
    "做法：采用的产品或技术方法",
    "产出：做出了什么结果"
  ],
  coverImage: "/assets/example-cover.jpg",
  coverAlt: "项目封面说明",
  visibility: "public",
  technologies: ["React", "Agent"],
  readme: "# 项目名称\\n\\n项目的公开说明。"
}
```

注意：

- `id` 和 `slug` 不要与已有项目重复，详情页地址使用 `slug`。
- `summary` 和 `highlights` 写给访客看，不要写成文件名或源码目录说明。
- 推荐按照“目标、做法、产出”写 `highlights`，每条尽量是一句话。
- `visibility: "public"` 才会出现在访客页面；私密项目只在管理模式可见。
- 不要把 `resume` 或包含手机号、密钥、内部文件的仓库设置为公开展示。

## 4. 添加项目图片

1. 把图片放进 `public/assets/`。
2. 文件名使用英文、数字和连字符，例如 `example-cover.jpg`。
3. 在项目数据中使用 `/assets/example-cover.jpg`，不要使用电脑上的绝对路径。
4. 建议上传前压缩图片；项目封面宽度约 900px，单张图片尽量控制在 1MB 以内。
5. `coverAlt` 要描述图片内容，方便无障碍阅读。

当前 BMO 黑客松照片位于：

- `public/assets/bmo-hackathon-device.jpg`
- `public/assets/bmo-hackathon-character.jpg`

## 5. 修改公开 README

公开 README 的初始正文在 `src/productData.js` 的 `initialReadmeMarkdown`。正文使用 Markdown：

- 一级标题使用 `#`。
- 分段标题使用 `##` 或 `###`。
- 项目名称可以使用 `**项目名称**` 加粗。
- 每段只表达一个主题，避免把 README 写成源码说明。

修改 README 后，如果本地浏览器仍显示旧版本，需要同时升级 `readmeStorageKey` 和 `INITIAL_README_STATE` 中的版本号，例如从 `v5` 改为 `v6`。这是因为浏览器会保存之前的 README 草稿和发布状态。

## 6. 删除项目

如果项目来自初始数据，先从 `GITHUB_PROJECTS` 删除对应对象。如果这个项目已经被浏览器或服务端保存过，还要在 `buildProjectDigest` 的过滤列表中加入它的 `slug`，否则旧缓存可能让它重新出现。

删除前请确认不是误删 GitHub 原仓库。网站里的删除只代表不在这个个人主页展示，不会删除 GitHub 仓库。

## 7. 个人信息和联系方式

个人资料编辑 `src/content/tulip-profile.json`。公开联系方式放在根级别的 `contact` 字段中：

```json
{
  "contact": {
    "tara_url": "https://example.com/tara-profile",
    "xiaohongshu_url": "https://example.com/xiaohongshu-profile"
  }
}
```

不要把手机号、邮箱密码、API Key、GitHub Token 或私有仓库内容写入公开资料。

## 8. GitHub 更新和部署

日常可以继续在 GitHub 修改项目 README 或代码。部署后，通过管理台的 GitHub 导入/同步读取公开内容；网站会把内容整理成访客可读的项目摘要。GitHub 更新后是否立即显示，取决于部署的同步接口和缓存策略。

本地前端预览使用浏览器存储；生产环境再接入 Cloudflare Pages/Workers、D1 和 R2。没有配置 AI 也可以运行，AI 只属于可选的 README 辅助能力。

## 9. 不要直接修改的目录

- `node_modules/`：依赖安装目录，运行 `npm install` 自动生成。
- `dist/`：构建产物，运行 `npm run build` 自动生成。
- `.git/`：版本控制目录。
- `.env` 和 `.env.*`：不要打包、不要上传密钥。

每次内容修改后，先运行本地预览确认页面，再执行 `npm run build` 生成部署版本。打包时使用项目根目录源码包，不需要把 `node_modules` 和 `dist` 放入压缩包。
